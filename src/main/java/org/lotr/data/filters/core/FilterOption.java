package org.lotr.data.filters.core;

public enum FilterOption {
    MATCH,
    NEGATE_MATCH,
    INCLUDE,
    EXCLUDE,
    EXISTS,
    DOES_NOT_EXIST,
    REGEX,
    LESS_THAN,

    GREATER_THAN,
    GREATER_THAN_OR_EQUAL_TO
}
